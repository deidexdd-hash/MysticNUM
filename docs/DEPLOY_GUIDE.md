# 🚀 ГАЙД ПО ДЕПЛОЮ НА RENDER

## Шаг 1: Подготовка проекта

### 1.1. Создайте Git репозиторий

```bash
# Инициализируйте Git (если еще не сделали)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: MysticNUM Ultimate v5"
```

### 1.2. Создайте репозиторий на GitHub

1. Перейдите на https://github.com
2. Нажмите "New repository"
3. Название: `mysticnum-ultimate`
4. Описание: `Профессиональная система нумерологии`
5. Public или Private (на ваш выбор)
6. НЕ создавайте README, .gitignore (они уже есть)
7. Нажмите "Create repository"

### 1.3. Загрузите код на GitHub

```bash
# Добавьте remote
git remote add origin https://github.com/ВАШ_USERNAME/mysticnum-ultimate.git

# Отправьте код
git branch -M main
git push -u origin main
```

---

## Шаг 2: Деплой на Render

### 2.1. Создайте аккаунт на Render

1. Перейдите на https://render.com
2. Нажмите "Get Started for Free"
3. Войдите через GitHub
4. Авторизуйте Render для доступа к репозиториям

### 2.2. Создайте новый Web Service

1. В дашборде Render нажмите "New +"
2. Выберите "Web Service"
3. Подключите ваш GitHub репозиторий `mysticnum-ultimate`
4. Нажмите "Connect"

### 2.3. Настройте Web Service

**Name:** `mysticnum-ultimate` (или любое другое имя)

**Environment:** `Node`

**Region:** `Frankfurt (EU Central)` (ближайший к России)

**Branch:** `main`

**Build Command:** `npm install`

**Start Command:** `npm start`

**Plan:** `Free` (для начала)

**Advanced:**
- Environment Variables:
  - `NODE_ENV` = `production`

### 2.4. Деплой

1. Нажмите "Create Web Service"
2. Render автоматически:
   - Клонирует репозиторий
   - Установит зависимости
   - Запустит сервер
3. Процесс займет 2-5 минут

### 2.5. Проверка

После успешного деплоя:
- Вы получите URL: `https://mysticnum-ultimate.onrender.com`
- Откройте его в браузере
- Проверьте работу приложения

---

## Шаг 3: Настройка (опционально)

### 3.1. Настройка Custom Domain

1. В настройках Web Service перейдите в "Settings"
2. Найдите раздел "Custom Domain"
3. Добавьте свой домен
4. Настройте DNS записи у регистратора

### 3.2. Настройка Health Checks

Render автоматически проверяет `/` каждые 30 секунд.

### 3.3. Настройка Auto-Deploy

По умолчанию включен:
- Каждый push в `main` → автоматический деплой

Отключить:
1. Settings → Build & Deploy
2. Auto-Deploy: Off

---

## Шаг 4: Обновление приложения

### Через Git:

```bash
# Внесите изменения в код
# ...

# Коммит
git add .
git commit -m "Update: описание изменений"

# Push
git push origin main
```

Render автоматически задеплоит новую версию.

### Через Dashboard:

1. Перейдите в ваш Web Service
2. Нажмите "Manual Deploy" → "Deploy latest commit"

---

## Шаг 5: Мониторинг

### Логи

1. В Dashboard → ваш Web Service
2. Вкладка "Logs"
3. Смотрите real-time логи

### Метрики

1. Вкладка "Metrics"
2. CPU, Memory, Response Time
3. Requests per second

---

## 🎯 ЧЕКЛИСТ ДЕПЛОЯ

- [ ] Git репозиторий создан
- [ ] Код загружен на GitHub
- [ ] Render аккаунт создан
- [ ] Web Service создан
- [ ] Environment variables настроены
- [ ] Деплой успешен
- [ ] Приложение открывается по URL
- [ ] Проверена работа всех модулей
- [ ] Проверен расчет матрицы
- [ ] Проверен экспорт PDF

---

## ⚠️ ВАЖНО: Ограничения Free плана

### Бесплатный план Render:

✅ **Включено:**
- 750 часов в месяц
- Автоматический SSL (HTTPS)
- Автоматические деплои
- 512 MB RAM
- 0.1 CPU
- Неограниченный трафик

⚠️ **Ограничения:**
- Сервис "засыпает" после 15 минут неактивности
- Первый запрос после сна: ~30 секунд
- Нет persistent storage

### Решения:

1. **UptimeRobot** - пингует сайт каждые 5 минут (не дает заснуть)
2. **Paid план** ($7/мес) - без сна, больше ресурсов

---

## 🔧 TROUBLESHOOTING

### Проблема: Build Failed

**Решение:**
1. Проверьте `package.json`
2. Убедитесь, что `engines.node >= 18.0.0`
3. Проверьте логи билда

### Проблема: Application Error

**Решение:**
1. Проверьте логи
2. Убедитесь, что `server.js` использует `process.env.PORT`
3. Проверьте `startCommand: npm start`

### Проблема: 404 на всех страницах

**Решение:**
1. Убедитесь, что все файлы загружены в Git
2. Проверьте структуру файлов
3. Проверьте пути в `server.js`

### Проблема: Долгая загрузка

**Решение:**
1. Минифицируйте CSS/JS
2. Оптимизируйте изображения
3. Включите кэширование
4. Рассмотрите Paid план

---

## 📚 ПОЛЕЗНЫЕ ССЫЛКИ

- Render Docs: https://render.com/docs
- Node.js на Render: https://render.com/docs/deploy-node-express-app
- Troubleshooting: https://render.com/docs/troubleshooting-deploys

---

## 🎉 ПОЗДРАВЛЯЕМ!

Ваше приложение **MysticNUM Ultimate** теперь доступно онлайн!

Делитесь ссылкой:
`https://ваше-имя.onrender.com`

---

**Автор гайда:** Claude (Anthropic)  
**Дата:** 07 февраля 2026
