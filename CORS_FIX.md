# ⚠️ ВАЖНО: Решение проблемы CORS

## Проблема

При открытии `index.html` напрямую (двойной клик) может возникнуть ошибка:
```
Access to fetch ... has been blocked by CORS policy
```

Это ограничение безопасности браузера для локальных файлов.

---

## ✅ Решение: Используйте локальный сервер

### Вариант 1: Python (РЕКОМЕНДУЕТСЯ)

**Windows:**
```
1. Двойной клик на START_SERVER.bat
2. Откройте в браузере: http://localhost:8000
```

**Mac/Linux:**
```bash
./start_server.sh
# Или
python3 -m http.server 8000
```

Затем откройте: **http://localhost:8000**

---

### Вариант 2: Node.js

Если у вас установлен Node.js:

```bash
npx serve
```

Или установите глобально:
```bash
npm install -g serve
serve
```

---

### Вариант 3: VS Code (Live Server)

1. Откройте папку в VS Code
2. Установите расширение "Live Server"
3. Правый клик на `index.html` → "Open with Live Server"

---

### Вариант 4: PHP

Если установлен PHP:
```bash
php -S localhost:8000
```

---

### Вариант 5: Браузер с отключенной защитой (НЕ РЕКОМЕНДУЕТСЯ)

**Chrome (только для тестирования!):**
```bash
chrome.exe --allow-file-access-from-files
```

⚠️ **НЕ используйте этот способ для постоянной работы!**

---

## 🌐 Вариант 6: Размещение онлайн

Если не хотите использовать локальный сервер, разместите проект онлайн:

### GitHub Pages (БЕСПЛАТНО):

1. Создайте репозиторий на GitHub
2. Загрузите ВСЕ файлы проекта
3. Settings → Pages → Source: main branch
4. Готово! Получите ссылку типа: `https://username.github.io/repository/`

### Netlify (БЕСПЛАТНО):

1. Зайдите на netlify.com
2. Drag & Drop папку проекта
3. Готово! Получите ссылку

### Vercel (БЕСПЛАТНО):

1. Зайдите на vercel.com
2. Import проекта
3. Deploy

---

## 💡 Почему нужен сервер?

Браузеры блокируют прямой доступ к локальным файлам (JSON, изображения) из-за политики безопасности CORS. Локальный веб-сервер решает эту проблему, предоставляя файлы по протоколу HTTP вместо FILE.

---

## ✅ Проверка работы

После запуска сервера вы должны увидеть:

1. ✅ Звездный фон
2. ✅ Заголовок "MysticNUM COMPLETE"  
3. ✅ 7 вкладок навигации
4. ✅ Форму ввода даты
5. ✅ В консоли (F12): "📚 База знаний загружена"

Если видите это - всё работает правильно!

---

## 🔧 Альтернатива: Встроенная база

Приложение теперь имеет **встроенную минимальную базу знаний**, которая работает даже через `file://` протокол. Однако для полного функционала рекомендуется использовать локальный сервер.

---

## 📞 Помощь

Если ничего не помогло:

1. Откройте консоль браузера (F12)
2. Скопируйте все ошибки
3. Проверьте версию браузера (нужен Chrome 90+, Firefox 88+, Safari 14+)
4. Попробуйте другой браузер

---

**Рекомендация:** Используйте `START_SERVER.bat` (Windows) или `start_server.sh` (Mac/Linux) для быстрого запуска!
